# ── JWT authentication ───────────────────────────────────────────────────────
# Use JWT_SECRET_KEY — NOT "SECRET_KEY" (config.py and CI expect this name)
JWT_SECRET_KEY=your-secret-key-here-change-in-production
