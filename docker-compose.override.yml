services:
  backend:
    environment:
      DATABASE_URL: postgresql+asyncpg://hvac_user:${POSTGRES_PASSWORD}@postgres:5432/hvac_intel
      REDIS_URL: redis://redis:6379/0
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  celery-worker:
    environment:
      DATABASE_URL: postgresql+asyncpg://hvac_user:${POSTGRES_PASSWORD}@postgres:5432/hvac_intel
      REDIS_URL: redis://redis:6379/0
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
